<%= form_for(@initiative, :url => initiative_path(@initiative), :method => :patch, :validate => true) do |f| %>
  <table>
	<%= render :partial => "initiative_edit", :locals => {:f => f} %>
    <tr>
      <td/>
      <td><%= f.submit "Update initiative", :disable_with => "Processing…" %></td>
    </tr>
  </table>
<% end %>