<%= form_for(@committee, :url => committees_path, :validate => true) do |f| %>
  <table>
	<%= render :partial => "committee_edit", :locals => {:f => f} %>
    <tr>
      <td/>
      <td><%= f.submit "Add committee", data: { disable_with: 'Processing…' } %></td>
    </tr>
  </table>
<% end %>